info:
  name: POST /api/v1/learn/subjects/{subjectId}/chapters/{chapterId}/vocabulary
  type: http
  seq: 2
  tags:
    - Learn – Vocabulary

http:
  method: POST
  url: "{{baseUrl}}/api/v1/learn/subjects/:subjectId/chapters/:chapterId/vocabulary"
  params:
    - name: subjectId
      value: "{{subjectId}}"
      type: path
    - name: chapterId
      value: "{{chapterId}}"
      type: path
  body:
    type: json
    data: |-
      {
        "title": "Begrüßungen",
        "orderIndex": 1,
        "rewardPoints": 10,
        "config": {
          "direction": "front-to-back",
          "sourceLang": "en",
          "targetLang": "de",
          "items": [
            {
              "front": "hello",
              "back": "hallo"
            },
            {
              "front": "goodbye",
              "back": "tschüss"
            }
          ]
        }
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        function capture(name, value) {
          if (value === undefined || value === null || value === '') return;
          bru.setEnvVar(name, String(value));
        }

        if (body && typeof body === 'object' && !Array.isArray(body)) {
          capture('subjectId', body.subjectId ?? body.id);
          capture('chapterId', body.chapterId ?? body.id);
          capture('vocabularyId', body.vocabularyId ?? body.id);
          capture('fatherId', body.fatherId);
          capture('childId', body.childId);
          capture('subjectId', body.subjectId);
          capture('chapterId', body.chapterId);
          capture('categoryId', body.categoryId);
          capture('exerciseId', body.exerciseId);
          capture('planId', body.planId);
          capture('positionId', body.positionId);
          capture('sessionId', body.sessionId);
          capture('attemptId', body.attemptId);
          capture('tagId', body.tagId);
          capture('classTestId', body.classTestId);
          capture('missionId', body.missionId);
          capture('achievementId', body.achievementId);
          capture('rewardId', body.rewardId);
          capture('redemptionId', body.redemptionId);
          capture('articleId', body.articleId);
          capture('listingId', body.listingId);
          capture('purchaseId', body.purchaseId);
          capture('activationId', body.activationId);
          capture('vocabularyId', body.vocabularyId);
          capture('wordId', body.wordId);
          capture('skinId', body.skinId);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Vokabel-Übung anlegen
    description: "Verifiziert durch DocsCaptureTests: Rolle `father`, erwartet HTTP 201."
    request:
      url: "{{baseUrl}}/api/v1/learn/subjects/:subjectId/chapters/:chapterId/vocabulary"
      method: POST
      params:
        - name: subjectId
          value: "{{subjectId}}"
          type: path
        - name: chapterId
          value: "{{chapterId}}"
          type: path
      body:
        type: json
        data: |-
          {
            "title": "Begrüßungen",
            "orderIndex": 1,
            "rewardPoints": 10,
            "config": {
              "direction": "front-to-back",
              "sourceLang": "en",
              "targetLang": "de",
              "items": [
                {
                  "front": "hello",
                  "back": "hallo"
                },
                {
                  "front": "goodbye",
                  "back": "tschüss"
                }
              ]
            }
          }
    response:
      status: 201
      statusText: Created
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": 13,
            "chapterId": 7,
            "type": "Vocabulary",
            "title": "Begrüßungen",
            "orderIndex": 1,
            "rewardPoints": 10,
            "createdAt": "<timestamp>",
            "config": {
              "direction": "front-to-back",
              "sourceLang": "en",
              "targetLang": "de",
              "refs": null,
              "items": []
            },
            "suggestedBonus": null,
            "gradeMin": null,
            "gradeMax": null,
            "schoolTypes": "None",
            "source": null,
            "categoryId": null,
            "categoryName": null,
            "authorFatherId": 1,
            "isOwn": true,
            "description": null,
            "defaultUseLeitner": false,
            "defaultRequireTypedTest": false
          }

docs: |-
  # POST /api/v1/learn/subjects/{subjectId}/chapters/{chapterId}/vocabulary

  `POST /api/v1/learn/subjects/{subjectId}/chapters/{chapterId}/vocabulary`
