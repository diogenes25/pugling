info:
  name: "Eigene Angebots-Sicht: Münzstand, verfügbare (aktive) Angebote und die eigenen Käufe."
  type: http
  seq: 4
  tags:
    - Me

http:
  method: GET
  url: "{{baseUrl}}/api/v1/me/rewards"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        function capture(name, value) {
          if (value === undefined || value === null || value === '') return;
          bru.setVar(name, value);
          bru.setEnvVar(name, String(value), { persist: true });
        }

        if (body && typeof body === 'object' && !Array.isArray(body)) {
          capture('rewardId', body.rewardId ?? body.id);
          capture('fatherId', body.fatherId);
          capture('childId', body.childId);
          capture('subjectId', body.subjectId);
          capture('chapterId', body.chapterId);
          capture('categoryId', body.categoryId);
          capture('exerciseId', body.exerciseId);
          capture('planId', body.planId);
          capture('positionId', body.positionId);
          capture('sessionId', body.sessionId);
          capture('attemptId', body.attemptId);
          capture('tagId', body.tagId);
          capture('classTestId', body.classTestId);
          capture('missionId', body.missionId);
          capture('achievementId', body.achievementId);
          capture('rewardId', body.rewardId);
          capture('redemptionId', body.redemptionId);
          capture('articleId', body.articleId);
          capture('listingId', body.listingId);
          capture('purchaseId', body.purchaseId);
          capture('activationId', body.activationId);
          capture('vocabularyId', body.vocabularyId);
          capture('wordId', body.wordId);
          capture('skinId', body.skinId);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Eigene Angebote & Käufe
    description: "Verifiziert durch DocsCaptureTests: Rolle `child`, erwartet HTTP 200."
    request:
      url: "{{baseUrl}}/api/v1/me/rewards"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "coins": 50,
            "available": [
              {
                "id": 1,
                "title": "30 Min Fernsehen",
                "cost": 200,
                "period": "Daily",
                "quantity": 2,
                "remainingThisPeriod": 2,
                "affordable": false,
                "planTitle": null,
                "exerciseTitle": null
              },
              {
                "id": 2,
                "title": "1 Stunde Zocken",
                "cost": 400,
                "period": "Weekly",
                "quantity": 5,
                "remainingThisPeriod": 5,
                "affordable": false,
                "planTitle": null,
                "exerciseTitle": null
              },
              {
                "id": 3,
                "title": "Taschengeld 5 €",
                "cost": 500,
                "period": "Weekly",
                "quantity": 1,
                "remainingThisPeriod": 1,
                "affordable": false,
                "planTitle": null,
                "exerciseTitle": null
              },
              {
                "id": 4,
                "title": "Kinoabend aussuchen",
                "cost": 1500,
                "period": "OneOff",
                "quantity": 1,
                "remainingThisPeriod": 1,
                "affordable": false,
                "planTitle": null,
                "exerciseTitle": null
              }
            ],
            "redemptions": []
          }

docs: |-
  # Eigene Angebots-Sicht: Münzstand, verfügbare (aktive) Angebote und die eigenen Käufe.

  `GET /api/v1/me/rewards`
