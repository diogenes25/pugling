info:
  name: Eigene Aktivierungsanfragen (neueste zuerst), optional nach Status gefiltert.
  type: http
  seq: 7
  tags:
    - Me

http:
  method: GET
  url: "{{baseUrl}}/api/v1/me/shop/activations"
  params:
    - name: status
      value: "{{status}}"
      type: query
    - name: skip
      value: "{{skip}}"
      type: query
    - name: take
      value: "{{take}}"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        function capture(name, value) {
          if (value === undefined || value === null || value === '') return;
          bru.setVar(name, value);
          bru.setEnvVar(name, String(value), { persist: true });
        }

        if (body && typeof body === 'object' && !Array.isArray(body)) {
          capture('activationId', body.activationId ?? body.id);
          capture('fatherId', body.fatherId);
          capture('childId', body.childId);
          capture('subjectId', body.subjectId);
          capture('chapterId', body.chapterId);
          capture('categoryId', body.categoryId);
          capture('exerciseId', body.exerciseId);
          capture('planId', body.planId);
          capture('positionId', body.positionId);
          capture('sessionId', body.sessionId);
          capture('attemptId', body.attemptId);
          capture('tagId', body.tagId);
          capture('classTestId', body.classTestId);
          capture('missionId', body.missionId);
          capture('achievementId', body.achievementId);
          capture('rewardId', body.rewardId);
          capture('redemptionId', body.redemptionId);
          capture('articleId', body.articleId);
          capture('listingId', body.listingId);
          capture('purchaseId', body.purchaseId);
          capture('activationId', body.activationId);
          capture('vocabularyId', body.vocabularyId);
          capture('wordId', body.wordId);
          capture('skinId', body.skinId);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Eigene Aktivierungen (Sohn)
    description: "Verifiziert durch DocsCaptureTests: Rolle `child`, erwartet HTTP 200."
    request:
      url: "{{baseUrl}}/api/v1/me/shop/activations"
      method: GET
      params:
        - name: status
          value: "{{status}}"
          type: query
        - name: skip
          value: "{{skip}}"
          type: query
        - name: take
          value: "{{take}}"
          type: query
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "id": 2,
              "shopArticleId": 5,
              "articleTitle": "Fernsehzeit (30 Min)",
              "unitType": "Minute",
              "actionType": "TV",
              "requestedQuantity": 10,
              "status": "Pending",
              "requestedAt": "2026-07-06T22:11:38.9058475",
              "closedAt": null
            },
            {
              "id": 1,
              "shopArticleId": 5,
              "articleTitle": "Fernsehzeit (30 Min)",
              "unitType": "Minute",
              "actionType": "TV",
              "requestedQuantity": 30,
              "status": "Pending",
              "requestedAt": "2026-07-06T22:11:38.8848482",
              "closedAt": null
            }
          ]

docs: |-
  # Eigene Aktivierungsanfragen (neueste zuerst), optional nach Status gefiltert.

  `GET /api/v1/me/shop/activations`
