info:
  name: "Eigenes aggregiertes Inventar: pro Artikel-Typ die verfügbare Gesamtmenge (nur was &gt; 0 ist). Gegenstück zum Aktivierungs-`POST` und zur Vater-Sicht (`children/{childId}/shop/inventory`); dieselben Daten stehen gebündelt auch in `GET me/shop`."
  type: http
  seq: 16
  tags:
    - Me

http:
  method: GET
  url: "{{baseUrl}}/api/v1/me/shop/inventory"
  params:
    - name: skip
      value: "0"
      type: query
      description: Anzahl übersprungener Einträge (Offset, Standard 0).
    - name: take
      value: "100"
      type: query
      description: Maximale Einträge (1..500). Gesamtzahl im Header `X-Total-Count`.
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        function capture(name, value) {
          if (value === undefined || value === null || value === '') return;
          bru.setEnvVar(name, String(value));
        }

        if (body && typeof body === 'object' && !Array.isArray(body)) {
          capture('fatherId', body.fatherId);
          capture('childId', body.childId);
          capture('subjectId', body.subjectId);
          capture('chapterId', body.chapterId);
          capture('categoryId', body.categoryId);
          capture('exerciseId', body.exerciseId);
          capture('planId', body.planId);
          capture('positionId', body.positionId);
          capture('sessionId', body.sessionId);
          capture('attemptId', body.attemptId);
          capture('tagId', body.tagId);
          capture('classTestId', body.classTestId);
          capture('missionId', body.missionId);
          capture('achievementId', body.achievementId);
          capture('rewardId', body.rewardId);
          capture('redemptionId', body.redemptionId);
          capture('articleId', body.articleId);
          capture('listingId', body.listingId);
          capture('purchaseId', body.purchaseId);
          capture('activationId', body.activationId);
          capture('vocabularyId', body.vocabularyId);
          capture('wordId', body.wordId);
          capture('skinId', body.skinId);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Eigenes Inventar (Sohn)
    description: "Verifiziert durch DocsCaptureTests: Rolle `child`, erwartet HTTP 200."
    request:
      url: "{{baseUrl}}/api/v1/me/shop/inventory"
      method: GET
      params:
        - name: skip
          value: "0"
          type: query
          description: Anzahl übersprungener Einträge (Offset, Standard 0).
        - name: take
          value: "100"
          type: query
          description: Maximale Einträge (1..500). Gesamtzahl im Header `X-Total-Count`.
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "shopArticleId": 5,
              "articleNumber": "TV-900",
              "title": "Fernsehzeit (30 Min)",
              "unitType": "Minute",
              "actionType": "TV",
              "quantity": 30
            }
          ]

docs: |-
  # Eigenes aggregiertes Inventar: pro Artikel-Typ die verfügbare Gesamtmenge (nur was &gt; 0 ist). Gegenstück zum Aktivierungs-`POST` und zur Vater-Sicht (`children/{childId}/shop/inventory`); dieselben Daten stehen gebündelt auch in `GET me/shop`.

  `GET /api/v1/me/shop/inventory`
