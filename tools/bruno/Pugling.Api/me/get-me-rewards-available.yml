info:
  name: Verfügbare (aktive) Angebote des Kindes, seitenweise.
  type: http
  seq: 9
  tags:
    - Me

http:
  method: GET
  url: "{{baseUrl}}/api/v1/me/rewards/available"
  params:
    - name: skip
      value: "0"
      type: query
      description: Anzahl zu überspringender Angebote (Paging).
    - name: take
      value: "100"
      type: query
      description: Maximale Angebots-Zahl (1..500). Gesamtzahl im Header `X-Total-Count`.
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        function capture(name, value) {
          if (value === undefined || value === null || value === '') return;
          bru.setEnvVar(name, String(value));
        }

        if (body && typeof body === 'object' && !Array.isArray(body)) {
          capture('rewardId', body.rewardId ?? body.id);
          capture('fatherId', body.fatherId);
          capture('childId', body.childId);
          capture('subjectId', body.subjectId);
          capture('chapterId', body.chapterId);
          capture('categoryId', body.categoryId);
          capture('exerciseId', body.exerciseId);
          capture('planId', body.planId);
          capture('positionId', body.positionId);
          capture('sessionId', body.sessionId);
          capture('attemptId', body.attemptId);
          capture('tagId', body.tagId);
          capture('classTestId', body.classTestId);
          capture('missionId', body.missionId);
          capture('achievementId', body.achievementId);
          capture('rewardId', body.rewardId);
          capture('redemptionId', body.redemptionId);
          capture('articleId', body.articleId);
          capture('listingId', body.listingId);
          capture('purchaseId', body.purchaseId);
          capture('activationId', body.activationId);
          capture('vocabularyId', body.vocabularyId);
          capture('wordId', body.wordId);
          capture('skinId', body.skinId);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Verfügbare Angebote (Liste)
    description: "Verifiziert durch DocsCaptureTests: Rolle `child`, erwartet HTTP 200."
    request:
      url: "{{baseUrl}}/api/v1/me/rewards/available"
      method: GET
      params:
        - name: skip
          value: "0"
          type: query
          description: Anzahl zu überspringender Angebote (Paging).
        - name: take
          value: "100"
          type: query
          description: Maximale Angebots-Zahl (1..500). Gesamtzahl im Header `X-Total-Count`.
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "id": 1,
              "title": "30 Min Fernsehen",
              "cost": 200,
              "period": "Daily",
              "quantity": 2,
              "remainingThisPeriod": 2,
              "affordable": false,
              "planTitle": null,
              "exerciseTitle": null
            },
            {
              "id": 2,
              "title": "1 Stunde Zocken",
              "cost": 400,
              "period": "Weekly",
              "quantity": 5,
              "remainingThisPeriod": 5,
              "affordable": false,
              "planTitle": null,
              "exerciseTitle": null
            },
            {
              "id": 3,
              "title": "Taschengeld 5 €",
              "cost": 500,
              "period": "Weekly",
              "quantity": 1,
              "remainingThisPeriod": 1,
              "affordable": false,
              "planTitle": null,
              "exerciseTitle": null
            },
            {
              "id": 4,
              "title": "Kinoabend aussuchen",
              "cost": 1500,
              "period": "OneOff",
              "quantity": 1,
              "remainingThisPeriod": 1,
              "affordable": false,
              "planTitle": null,
              "exerciseTitle": null
            }
          ]

docs: |-
  # Verfügbare (aktive) Angebote des Kindes, seitenweise.

  `GET /api/v1/me/rewards/available`
