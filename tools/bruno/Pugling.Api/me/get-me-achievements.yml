info:
  name: "Eigene Auszeichnungen (Badges): erreichte und noch offene, erreichte zuerst (reine Lesesicht), seitenweise."
  type: http
  seq: 1
  tags:
    - Me

http:
  method: GET
  url: "{{baseUrl}}/api/v1/me/achievements"
  params:
    - name: skip
      value: "0"
      type: query
      description: Anzahl zu überspringender Auszeichnungen (Paging).
    - name: take
      value: "100"
      type: query
      description: Maximale Auszeichnungs-Zahl (1..500). Gesamtzahl im Header `X-Total-Count`.
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        function capture(name, value) {
          if (value === undefined || value === null || value === '') return;
          bru.setEnvVar(name, String(value));
        }

        if (body && typeof body === 'object' && !Array.isArray(body)) {
          capture('achievementId', body.achievementId ?? body.id);
          capture('fatherId', body.fatherId);
          capture('childId', body.childId);
          capture('subjectId', body.subjectId);
          capture('chapterId', body.chapterId);
          capture('categoryId', body.categoryId);
          capture('exerciseId', body.exerciseId);
          capture('planId', body.planId);
          capture('positionId', body.positionId);
          capture('sessionId', body.sessionId);
          capture('attemptId', body.attemptId);
          capture('tagId', body.tagId);
          capture('classTestId', body.classTestId);
          capture('missionId', body.missionId);
          capture('achievementId', body.achievementId);
          capture('rewardId', body.rewardId);
          capture('redemptionId', body.redemptionId);
          capture('articleId', body.articleId);
          capture('listingId', body.listingId);
          capture('purchaseId', body.purchaseId);
          capture('activationId', body.activationId);
          capture('vocabularyId', body.vocabularyId);
          capture('wordId', body.wordId);
          capture('skinId', body.skinId);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Eigene Auszeichnungen (Liste)
    description: "Verifiziert durch DocsCaptureTests: Rolle `child`, erwartet HTTP 200."
    request:
      url: "{{baseUrl}}/api/v1/me/achievements"
      method: GET
      params:
        - name: skip
          value: "0"
          type: query
          description: Anzahl zu überspringender Auszeichnungen (Paging).
        - name: take
          value: "100"
          type: query
          description: Maximale Auszeichnungs-Zahl (1..500). Gesamtzahl im Header `X-Total-Count`.
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "id": 4,
              "title": "Feuer-Streak",
              "icon": "🔥",
              "metric": "StreakDays",
              "threshold": 7,
              "current": 0,
              "earned": false,
              "earnedAt": null,
              "rewardPoints": 70
            },
            {
              "id": 3,
              "title": "Test-Ass",
              "icon": "🏆",
              "metric": "TestsPassed",
              "threshold": 10,
              "current": 0,
              "earned": false,
              "earnedAt": null,
              "rewardPoints": 40
            },
            {
              "id": 1,
              "title": "Erste Schritte",
              "icon": "🌱",
              "metric": "CorrectReviews",
              "threshold": 50,
              "current": 0,
              "earned": false,
              "earnedAt": null,
              "rewardPoints": 20
            },
            {
              "id": 2,
              "title": "Wortschatz-Sammler",
              "icon": "📚",
              "metric": "NewWords",
              "threshold": 100,
              "current": 0,
              "earned": false,
              "earnedAt": null,
              "rewardPoints": 50
            },
            {
              "id": 5,
              "title": "Marathon",
              "icon": "⏱️",
              "metric": "MinutesPracticed",
              "threshold": 300,
              "current": 0,
              "earned": false,
              "earnedAt": null,
              "rewardPoints": 60
            }
          ]

docs: |-
  # Eigene Auszeichnungen (Badges): erreichte und noch offene, erreichte zuerst (reine Lesesicht), seitenweise.

  `GET /api/v1/me/achievements`
