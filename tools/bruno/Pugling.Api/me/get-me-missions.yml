info:
  name: Eigene Missionen (Tages-/Wochen-/Zusatzziele) mit aktuellem Fortschritt (reine Lesesicht).
  type: http
  seq: 2
  tags:
    - Me

http:
  method: GET
  url: "{{baseUrl}}/api/v1/me/missions"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        function capture(name, value) {
          if (value === undefined || value === null || value === '') return;
          bru.setVar(name, value);
          bru.setEnvVar(name, String(value), { persist: true });
        }

        if (body && typeof body === 'object' && !Array.isArray(body)) {
          capture('missionId', body.missionId ?? body.id);
          capture('fatherId', body.fatherId);
          capture('childId', body.childId);
          capture('subjectId', body.subjectId);
          capture('chapterId', body.chapterId);
          capture('categoryId', body.categoryId);
          capture('exerciseId', body.exerciseId);
          capture('planId', body.planId);
          capture('positionId', body.positionId);
          capture('sessionId', body.sessionId);
          capture('attemptId', body.attemptId);
          capture('tagId', body.tagId);
          capture('classTestId', body.classTestId);
          capture('missionId', body.missionId);
          capture('achievementId', body.achievementId);
          capture('rewardId', body.rewardId);
          capture('redemptionId', body.redemptionId);
          capture('articleId', body.articleId);
          capture('listingId', body.listingId);
          capture('purchaseId', body.purchaseId);
          capture('activationId', body.activationId);
          capture('vocabularyId', body.vocabularyId);
          capture('wordId', body.wordId);
          capture('skinId', body.skinId);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Eigene Missionen
    description: "Verifiziert durch DocsCaptureTests: Rolle `child`, erwartet HTTP 200."
    request:
      url: "{{baseUrl}}/api/v1/me/missions"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "id": 1,
              "title": "Tagesziel: 10 richtige Antworten",
              "metric": "CorrectReviews",
              "period": "Daily",
              "target": 10,
              "current": 0,
              "completed": false,
              "rewardPoints": 15
            },
            {
              "id": 2,
              "title": "Tagesziel: 15 Minuten üben",
              "metric": "MinutesPracticed",
              "period": "Daily",
              "target": 15,
              "current": 0,
              "completed": false,
              "rewardPoints": 10
            },
            {
              "id": 3,
              "title": "Wochenziel: 3 Tests bestehen",
              "metric": "TestsPassed",
              "period": "Weekly",
              "target": 3,
              "current": 0,
              "completed": false,
              "rewardPoints": 30
            },
            {
              "id": 4,
              "title": "Wochenziel: 25 neue Wörter",
              "metric": "NewWords",
              "period": "Weekly",
              "target": 25,
              "current": 0,
              "completed": false,
              "rewardPoints": 40
            }
          ]

docs: |-
  # Eigene Missionen (Tages-/Wochen-/Zusatzziele) mit aktuellem Fortschritt (reine Lesesicht).

  `GET /api/v1/me/missions`
