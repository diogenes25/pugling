info:
  name: "Startet einen Testversuch für die Position. Der Klausur-Modus ist strikt server-getrieben: der Start friert die Prüfungsreihenfolge ein und liefert nur die Metadaten – die Fragen holt der Client einzeln über Task&lt;ActionResult&lt;TestNextResponse&gt;&gt; PositionTestsController.Next(int planId, int positionId, int attemptId) und beantwortet sie über Task&lt;ActionResult&lt;AnswerAck&gt;&gt; PositionTestsController.Answer(int planId, int positionId, int attemptId, AnswerDto dto) (kein Zurück, Feedback erst bei Task&lt;ActionResult&lt;SubmitResponse&gt;&gt; PositionTestsController.Submit(int planId, int positionId, int attemptId, SubmitDto dto))."
  type: http
  seq: 1
  tags:
    - Study – Position Tests

http:
  method: POST
  url: "{{baseUrl}}/api/v1/study-plans/:planId/positions/:positionId/tests"
  params:
    - name: planId
      value: "{{planId}}"
      type: path
    - name: positionId
      value: "{{positionId}}"
      type: path
  body:
    type: json
    data: |-
      {}
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        function capture(name, value) {
          if (value === undefined || value === null || value === '') return;
          bru.setEnvVar(name, String(value));
        }

        if (body && typeof body === 'object' && !Array.isArray(body)) {
          capture('planId', body.planId ?? body.id);
          capture('positionId', body.positionId ?? body.id);
          capture('attemptId', body.attemptId ?? body.id);
          capture('fatherId', body.fatherId);
          capture('childId', body.childId);
          capture('subjectId', body.subjectId);
          capture('chapterId', body.chapterId);
          capture('categoryId', body.categoryId);
          capture('exerciseId', body.exerciseId);
          capture('planId', body.planId);
          capture('positionId', body.positionId);
          capture('sessionId', body.sessionId);
          capture('attemptId', body.attemptId);
          capture('tagId', body.tagId);
          capture('classTestId', body.classTestId);
          capture('missionId', body.missionId);
          capture('achievementId', body.achievementId);
          capture('rewardId', body.rewardId);
          capture('redemptionId', body.redemptionId);
          capture('articleId', body.articleId);
          capture('listingId', body.listingId);
          capture('purchaseId', body.purchaseId);
          capture('activationId', body.activationId);
          capture('vocabularyId', body.vocabularyId);
          capture('wordId', body.wordId);
          capture('skinId', body.skinId);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Test starten (Klausur, ohne Aufgaben-Bulk)
    description: "Verifiziert durch DocsCaptureTests: Rolle `child`, erwartet HTTP 201."
    request:
      url: "{{baseUrl}}/api/v1/study-plans/:planId/positions/:positionId/tests"
      method: POST
      params:
        - name: planId
          value: "{{planId}}"
          type: path
        - name: positionId
          value: "{{positionId}}"
          type: path
      body:
        type: json
        data: |-
          {}
    response:
      status: 201
      statusText: Created
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "attemptId": 1,
            "planId": 2,
            "positionId": 2,
            "day": "2026-07-07",
            "stage": 4,
            "totalItems": 1
          }
  - name: Test auf Übung ohne prüfbaren Inhalt (Fehlerfall)
    description: "Verifiziert durch DocsCaptureTests: Rolle `child`, erwartet HTTP 400, Code `no_checkable_content`."
    request:
      url: "{{baseUrl}}/api/v1/study-plans/:planId/positions/:positionId/tests"
      method: POST
      params:
        - name: planId
          value: "{{planId}}"
          type: path
        - name: positionId
          value: "{{positionId}}"
          type: path
      body:
        type: json
        data: |-
          {}
    response:
      status: 400
      statusText: Bad Request
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "type": "https://pugling.app/errors/no_checkable_content",
            "title": "No checkable content.",
            "status": 400,
            "detail": "The exercise contains no checkable content.",
            "code": "no_checkable_content",
            "traceId": "<trace-id>"
          }

docs: |-
  # Startet einen Testversuch für die Position. Der Klausur-Modus ist strikt server-getrieben: der Start friert die Prüfungsreihenfolge ein und liefert nur die Metadaten – die Fragen holt der Client einzeln über Task&lt;ActionResult&lt;TestNextResponse&gt;&gt; PositionTestsController.Next(int planId, int positionId, int attemptId) und beantwortet sie über Task&lt;ActionResult&lt;AnswerAck&gt;&gt; PositionTestsController.Answer(int planId, int positionId, int attemptId, AnswerDto dto) (kein Zurück, Feedback erst bei Task&lt;ActionResult&lt;SubmitResponse&gt;&gt; PositionTestsController.Submit(int planId, int positionId, int attemptId, SubmitDto dto)).

  `POST /api/v1/study-plans/{planId}/positions/{positionId}/tests`
