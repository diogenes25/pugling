info:
  name: Ändert ein Angebot partiell (Preis, Menge, Bestand, Aktiv-Status).
  type: http
  seq: 16
  tags:
    - Admin – Shop

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/shop/articles/:articleId/listings/:listingId"
  params:
    - name: articleId
      value: "{{articleId}}"
      type: path
    - name: listingId
      value: "{{listingId}}"
      type: path
  body:
    type: json
    data: |-
      {
        "currentStock": 5,
        "maxStock": 10
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        function capture(name, value) {
          if (value === undefined || value === null || value === '') return;
          bru.setEnvVar(name, String(value));
        }

        if (body && typeof body === 'object' && !Array.isArray(body)) {
          capture('articleId', body.articleId ?? body.id);
          capture('listingId', body.listingId ?? body.id);
          capture('fatherId', body.fatherId);
          capture('childId', body.childId);
          capture('subjectId', body.subjectId);
          capture('chapterId', body.chapterId);
          capture('categoryId', body.categoryId);
          capture('exerciseId', body.exerciseId);
          capture('planId', body.planId);
          capture('positionId', body.positionId);
          capture('sessionId', body.sessionId);
          capture('attemptId', body.attemptId);
          capture('tagId', body.tagId);
          capture('classTestId', body.classTestId);
          capture('missionId', body.missionId);
          capture('achievementId', body.achievementId);
          capture('rewardId', body.rewardId);
          capture('redemptionId', body.redemptionId);
          capture('articleId', body.articleId);
          capture('listingId', body.listingId);
          capture('purchaseId', body.purchaseId);
          capture('activationId', body.activationId);
          capture('vocabularyId', body.vocabularyId);
          capture('wordId', body.wordId);
          capture('skinId', body.skinId);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Angebot ändern (Bestand auffüllen)
    description: "Verifiziert durch DocsCaptureTests: Rolle `father`, erwartet HTTP 200."
    request:
      url: "{{baseUrl}}/api/v1/shop/articles/:articleId/listings/:listingId"
      method: PATCH
      params:
        - name: articleId
          value: "{{articleId}}"
          type: path
        - name: listingId
          value: "{{listingId}}"
          type: path
      body:
        type: json
        data: |-
          {
            "currentStock": 5,
            "maxStock": 10
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": 7,
            "shopArticleId": 5,
            "articleNumber": "TV-900",
            "articleTitle": "Fernsehzeit (30 Min)",
            "title": "30 Min Fernsehen",
            "description": "Einmalige Halbstunde",
            "coinPrice": 120,
            "gemPrice": 0,
            "unitsPerPurchase": 30,
            "active": true,
            "currentStock": 5,
            "maxStock": 10,
            "refillKind": "None",
            "refillAtUtc": null,
            "refillDayOfWeek": null,
            "lastRefilledAtUtc": null,
            "createdAt": "2026-07-07T22:33:06.9336927"
          }

docs: |-
  # Ändert ein Angebot partiell (Preis, Menge, Bestand, Aktiv-Status).

  `PATCH /api/v1/shop/articles/{articleId}/listings/{listingId}`
