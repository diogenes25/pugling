info:
  name: Storniert einen offenen Kauf und erstattet Coins/Gems zurück.
  type: http
  seq: 6
  tags:
    - Admin – Shop

http:
  method: POST
  url: "{{baseUrl}}/api/v1/children/:childId/shop/purchases/:purchaseId/cancel"
  params:
    - name: childId
      value: "{{childId}}"
      type: path
    - name: purchaseId
      value: "{{purchaseId}}"
      type: path
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        function capture(name, value) {
          if (value === undefined || value === null || value === '') return;
          bru.setEnvVar(name, String(value));
        }

        if (body && typeof body === 'object' && !Array.isArray(body)) {
          capture('childId', body.childId ?? body.id);
          capture('purchaseId', body.purchaseId ?? body.id);
          capture('fatherId', body.fatherId);
          capture('childId', body.childId);
          capture('subjectId', body.subjectId);
          capture('chapterId', body.chapterId);
          capture('categoryId', body.categoryId);
          capture('exerciseId', body.exerciseId);
          capture('planId', body.planId);
          capture('positionId', body.positionId);
          capture('sessionId', body.sessionId);
          capture('attemptId', body.attemptId);
          capture('tagId', body.tagId);
          capture('classTestId', body.classTestId);
          capture('missionId', body.missionId);
          capture('achievementId', body.achievementId);
          capture('rewardId', body.rewardId);
          capture('redemptionId', body.redemptionId);
          capture('articleId', body.articleId);
          capture('listingId', body.listingId);
          capture('purchaseId', body.purchaseId);
          capture('activationId', body.activationId);
          capture('vocabularyId', body.vocabularyId);
          capture('wordId', body.wordId);
          capture('skinId', body.skinId);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Kauf stornieren (Vater)
    description: "Verifiziert durch DocsCaptureTests: Rolle `father`, erwartet HTTP 200."
    request:
      url: "{{baseUrl}}/api/v1/children/:childId/shop/purchases/:purchaseId/cancel"
      method: POST
      params:
        - name: childId
          value: "{{childId}}"
          type: path
        - name: purchaseId
          value: "{{purchaseId}}"
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": 1,
            "childId": 6,
            "shopListingId": 7,
            "articleNumber": "TV-900",
            "title": "30 Min Fernsehen",
            "description": "Einmalige Halbstunde",
            "coinPrice": 120,
            "gemPrice": 0,
            "unitsPerPurchase": 30,
            "status": "Cancelled",
            "purchasedAt": "2026-07-07T21:03:49.8639377",
            "closedAt": "<timestamp>",
            "canCancel": false
          }

docs: |-
  # Storniert einen offenen Kauf und erstattet Coins/Gems zurück.

  `POST /api/v1/children/{childId}/shop/purchases/{purchaseId}/cancel`
