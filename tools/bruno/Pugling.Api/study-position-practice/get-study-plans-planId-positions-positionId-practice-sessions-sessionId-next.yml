info:
  name: Liefert die aktuelle Karte an der Cursor-Position der Sitzung (Lern-Modus, One-at-a-time). Übersprungen werden seit dem Start entfernte Items. Ist der Cursor am Ende, kommt bool NextResponse.Done.
  type: http
  seq: 6
  tags:
    - Study – Position Practice

http:
  method: GET
  url: "{{baseUrl}}/api/v1/study-plans/:planId/positions/:positionId/practice-sessions/:sessionId/next"
  params:
    - name: planId
      value: "{{planId}}"
      type: path
    - name: positionId
      value: "{{positionId}}"
      type: path
    - name: sessionId
      value: "{{sessionId}}"
      type: path
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();

        function capture(name, value) {
          if (value === undefined || value === null || value === '') return;
          bru.setEnvVar(name, String(value));
        }

        if (body && typeof body === 'object' && !Array.isArray(body)) {
          capture('planId', body.planId ?? body.id);
          capture('positionId', body.positionId ?? body.id);
          capture('sessionId', body.sessionId ?? body.id);
          capture('fatherId', body.fatherId);
          capture('childId', body.childId);
          capture('subjectId', body.subjectId);
          capture('chapterId', body.chapterId);
          capture('categoryId', body.categoryId);
          capture('exerciseId', body.exerciseId);
          capture('planId', body.planId);
          capture('positionId', body.positionId);
          capture('sessionId', body.sessionId);
          capture('attemptId', body.attemptId);
          capture('tagId', body.tagId);
          capture('classTestId', body.classTestId);
          capture('missionId', body.missionId);
          capture('achievementId', body.achievementId);
          capture('rewardId', body.rewardId);
          capture('redemptionId', body.redemptionId);
          capture('articleId', body.articleId);
          capture('listingId', body.listingId);
          capture('purchaseId', body.purchaseId);
          capture('activationId', body.activationId);
          capture('vocabularyId', body.vocabularyId);
          capture('wordId', body.wordId);
          capture('skinId', body.skinId);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Nächste Karte (server-geführter Cursor)
    description: "Verifiziert durch DocsCaptureTests: Rolle `child`, erwartet HTTP 200."
    request:
      url: "{{baseUrl}}/api/v1/study-plans/:planId/positions/:positionId/practice-sessions/:sessionId/next"
      method: GET
      params:
        - name: planId
          value: "{{planId}}"
          type: path
        - name: positionId
          value: "{{positionId}}"
          type: path
        - name: sessionId
          value: "{{sessionId}}"
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "card": {
              "itemIndex": 0,
              "stage": 4,
              "type": "Vocabulary",
              "prompt": "hello",
              "hint": null,
              "answerLength": null,
              "reveal": null,
              "choices": null,
              "audioUrl": null
            },
            "done": false,
            "cursor": 0,
            "total": 2
          }

docs: |-
  # Liefert die aktuelle Karte an der Cursor-Position der Sitzung (Lern-Modus, One-at-a-time). Übersprungen werden seit dem Start entfernte Items. Ist der Cursor am Ende, kommt bool NextResponse.Done.

  `GET /api/v1/study-plans/{planId}/positions/{positionId}/practice-sessions/{sessionId}/next`
