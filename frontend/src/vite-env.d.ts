/// <reference types="vite/client" />

// Typen für `import.meta.env` (u. a. das `DEV`-Flag, an dem das Anmerkungs-Widget hängt) und für
// Vite-eigene Asset-Importe. Gehört normalerweise zum Vite-Template; hier nachgezogen, weil
// `import.meta.env` erstmals gebraucht wird.
